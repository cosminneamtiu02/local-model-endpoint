[importlinter]
root_package = app

# Architecture contracts for the LIP project.
#
# Layer-level forbidden contracts that work even before the LIP feature
# is scaffolded under app.features. Feature-internal layering contracts
# for app.features.inference fill in as features land:
#   - LIP-E001-F001 (#10): model/ + schemas/, plus the
#     inference-model-no-schemas contract below.
#   - LIP-E003-F001 (this PR): repository/ (ollama_client.py).
# The full router -> service -> repository -> model chain is enforced
# once LIP-E001-F002 (orchestration) and LIP-E003-F002 (typed adapter
# methods) land their layers; feature-isolation contracts (features
# can't import each other) get a populated forbidden_modules entry
# when a second feature appears under app.features.

[importlinter:contract:core-no-features]
name = Core layer cannot import from features
type = forbidden
source_modules =
    app.core
forbidden_modules =
    app.features

[importlinter:contract:exceptions-no-features]
name = Exceptions cannot import from features
type = forbidden
source_modules =
    app.exceptions
forbidden_modules =
    app.features

[importlinter:contract:schemas-no-features]
name = Schemas cannot import from features
type = forbidden
source_modules =
    app.schemas
forbidden_modules =
    app.features

# Note: app.api can import from app.features because it includes feature
# routers; it is intentionally excluded from the forbidden list above.
#
# Note: We don't use a generated-exceptions-isolation contract because
# import-linter traces transitive imports. The discipline is enforced by
# CLAUDE.md: "Never import from exceptions._generated directly."

# ── inference feature ──────────────────────────────────────────────
# Active layers under app.features.inference: model/, schemas/
# (LIP-E001-F001), repository/ (LIP-E003-F001 + LIP-E003-F002). The
# router -> service edge will land alongside LIP-E001-F002.
#
# CLAUDE.md "schemas may import value-objects from model/, models
# never import schemas" rule:
[importlinter:contract:inference-model-no-schemas]
name = inference: model layer cannot import from schemas layer
type = forbidden
source_modules =
    app.features.inference.model
forbidden_modules =
    app.features.inference.schemas

# Repository is the data-access boundary — for LIP it talks to Ollama,
# not a database. It must not depend on the wire-shape schemas above
# (which are response-envelope concerns owned by the service+router).
[importlinter:contract:inference-repository-no-schemas]
name = inference: repository layer cannot import from schemas layer
type = forbidden
source_modules =
    app.features.inference.repository
forbidden_modules =
    app.features.inference.schemas

# Layer direction rule: repository -> model is permitted (LIP-E003-F002
# made this edge load-bearing — OllamaClient.chat returns OllamaChatResult
# from model/, and ollama_translation.py imports it). The inverse
# direction (model -> repository) would invert the layering: value
# objects must not know about the data-access boundary.
[importlinter:contract:inference-model-no-repository]
name = inference: model layer cannot import from repository layer
type = forbidden
source_modules =
    app.features.inference.model
forbidden_modules =
    app.features.inference.repository
